name: noise_meter_app
description: An application to measure the noise.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  # icons
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # static analyzer
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
